# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# app (not package): keep loose until we pin deliberately
pubspec.lock
# NOTE: generated code (drift *.g.dart, freezed, gen_l10n) IS committed on purpose so the repo
# builds/runs straight from a clean clone without a build_runner step (no CI yet). Do not ignore it.
coverage/

# IDE
.idea/
.vscode/
*.iml

# iOS
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
**/*.xcworkspace/xcuserdata/

# Android
android/.gradle/
android/local.properties
android/key.properties
*.keystore
*.jks

# macOS
.DS_Store

# Secrets — never commit
*.env
secrets/
